/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package util;

import java.awt.image.BufferedImage;
import java.io.File;
import javafx.scene.image.Image;
import javax.imageio.ImageIO;
import javax.imageio.stream.FileImageInputStream;
import javax.imageio.stream.ImageInputStream;

/**
 *
 * @author Raul
 */
public class byteImage {
    
   /*  File imginstalacion = new File(Controller.getController().configAplication.getRootElement().getChild("instalaciones").getValue());
        File filefo1 = new File(imginstalacion.getAbsolutePath());
        ImageInputStream imgfo1 = new FileImageInputStream(filefo1);
        BufferedImage imagefo1 = ImageIO.read(imgfo1);
        byte[] imgBytefo1 = ConversionImage.BufferedImage_to_Byte_Array_JPG(imagefo1);
        Image imgShowfo1 = ConversionImage.getImagefromByteArray(imgBytefo1);
        imgIns.setImage(imgShowfo1);*/
}
